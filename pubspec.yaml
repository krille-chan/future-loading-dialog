name: future_loading_dialog
description: Easy to use adaptive loading dialog to visualize a Dart Future.
version: 0.1.1
homepage: https://famedly.com
repository: https://gitlab.com/famedly/libraries/future_loading_dialog

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  pedantic: ^1.9.0
  flutter_test:
    sdk: flutter
